{
  "name": "surrey-organists",
  "version": "1.0.0",
  "description": "Surrey Organists' Association website",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port=8765",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it": "^14.0.0",
    "wrangler": "^4.103.0"
  }
}
